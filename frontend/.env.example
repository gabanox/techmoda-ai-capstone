# API Configuration
#
# Sandbox AWS re/Start: la API base es una Lambda Function URL (NO API Gateway).
# Pegá aquí la salida `ApiUrl` del stack (sam deploy / CloudFormation Outputs).
# Formato: https://<id>.lambda-url.us-west-2.on.aws
# (el slash final es opcional; el código lo normaliza)
#
# Ejemplo:
# VITE_API_URL=https://abcdef1234567890.lambda-url.us-west-2.on.aws
VITE_API_URL=https://your-function-url-id.lambda-url.us-west-2.on.aws
